using System.IO;
using System;

namespace fileAndDirtoryReadWirte
{
    class Program
    {
        /// <summary>
        /// 判断是否目录或文件是否存在
        /// </summary>
        /// <param name="path">路径</param>
        /// <param name="name">文件名</param>
        /// <returns>结果</returns>
        static bool IsExistDirOrFile(string path,string name)
        {
            
        }

        /// <summary>
        /// 判断是否目录或文件是否存在
        /// </summary>
        /// <param name="name">文件名</param>
        /// <returns>结果</returns>
        static bool IsExistDirOrFile(string name)
        {
            
        }

        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
